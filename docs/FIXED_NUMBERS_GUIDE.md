# 固定号码分析使用指南

## 📋 功能概述

**固定号码分析**（Fixed Numbers Analysis）允许您：
- 📝 输入自己选择的固定号码（如生日、幸运数字等）
- 📊 查看这些号码的历史表现和统计信息
- ⚖️ 评估号码组合的合理性（奇偶比、大小比平衡）
- 🎲 基于固定号码生成补充组合建议

---

## 🚀 快速开始

### 命令行使用

#### 1️⃣ 分析固定号码

**双色球示例**:
```bash
# 分析红球 07, 18, 25 和蓝球 14
python scripts/generate_fixed_numbers.py --type ssq --fixed-red 07,18,25 --fixed-blue 14

# 只分析红球（不固定蓝球）
python scripts/generate_fixed_numbers.py --type ssq --fixed-red 03,11,22

# 只分析蓝球
python scripts/generate_fixed_numbers.py --type ssq --fixed-blue 09
```

**大乐透示例**:
```bash
# 分析前区 05, 12, 23 和后区 03, 09
python scripts/generate_fixed_numbers.py --type dlt --fixed-red 05,12,23 --fixed-blue 03,09

# 只分析前区
python scripts/generate_fixed_numbers.py --type dlt --fixed-red 07,18,25,30,33
```

#### 2️⃣ 生成推荐组合

**基于固定号码生成**:
```bash
# 生成3组组合，保持固定号码，随机补充其他号码
python scripts/generate_fixed_numbers.py --type ssq --fixed-red 07,18,25 --fixed-blue 14 --generate --count 3

# 使用热号权重模式（优先选择热门号码补充）
python scripts/generate_fixed_numbers.py --type ssq --fixed-red 07,18 --generate --count 5 --mode weighted
```

**纯随机生成**（无固定号码）:
```bash
# 生成5组完全随机的号码
python scripts/generate_fixed_numbers.py --type ssq --generate --count 5
```

---

## 📊 分析报告内容

### 1. 号码历史表现

对于每个固定号码，系统会分析：

| 指标 | 说明 |
|------|------|
| **出现次数** | 该号码在历史开奖中出现的总次数 |
| **频率** | 出现次数占总期数的百分比 |
| **当前遗漏** | 距离上次出现已间隔多少期 |
| **状态** | 热号/正常/冷号（基于出现频率判断） |

**状态判断标准**:
- 🔥 **热号**: 出现频率 > 15%（红球）或 > 10%（蓝球）
- ✅ **正常**: 出现频率在正常范围内
- ❄️ **冷号**: 出现频率 < 8%（红球）

### 2. 组合评估

系统会评估您的固定号码组合：

**奇偶比评估**:
- 理想的奇偶比：3:3（双色球红球）
- 评分标准：
  - ⭐⭐ 奇偶数量差 ≤ 1（优秀）
  - ⭐ 奇偶数量差 > 1（需调整）

**大小比评估**:
- 双色球大小分界：17（1-16小，17-33大）
- 大乐透大小分界：18（1-17小，18-35大）
- 评分标准同上

**综合评分**:
- ⭐⭐⭐⭐⭐ (4/4分): 奇偶和大小都平衡
- ⭐⭐⭐ (3/4分): 一项平衡，一项稍差
- ⭐⭐ (2/4分): 两项都需要调整

### 3. 推荐组合

基于您的固定号码，系统会生成完整的投注组合：

**示例**:
```
固定号码: 红球 07, 18, 25 + 蓝球 14

组合 1:
🔴 红球: 07 17 18 20 23 25（补充: 17, 20, 23）
🔵 蓝球: 14

组合 2:
🔴 红球: 07 12 18 24 25 30（补充: 12, 24, 30）
🔵 蓝球: 14
```

**补充号码的选择**:
- **随机模式** (--mode random): 完全随机选择
- **热号权重模式** (--mode weighted): 优先选择近期热门号码

---

## 💡 使用场景

### 场景1: 生日号码分析

您的生日是 1990年7月18日，想用 07, 18, 19, 90(→33)作为固定号码：

```bash
python scripts/generate_fixed_numbers.py \
  --type ssq \
  --fixed-red 07,18,19,33 \
  --fixed-blue 09 \
  --generate \
  --count 3
```

### 场景2: 守号分析

您一直守号 05, 12, 23，想看看这些号码的历史表现：

```bash
python scripts/generate_fixed_numbers.py \
  --type ssq \
  --fixed-red 05,12,23 \
  --fixed-blue 08
```

### 场景3: 胆拖投注参考

您确定3个胆码，需要选择3个拖码：

```bash
python scripts/generate_fixed_numbers.py \
  --type ssq \
  --fixed-red 09,15,27 \
  --generate \
  --count 10 \
  --mode weighted
```

---

## ⚠️ 重要说明

### 1. 号码验证规则

**双色球**:
- 红球：1-33，不重复，最多6个
- 蓝球：1-16，1个

**大乐透**:
- 前区：1-35，不重复，最多5个
- 后区：1-12，最多2个

### 2. 分析局限性

- 📊 **历史表现 ≠ 未来预测**: 历史数据不能预测未来开奖
- 🎲 **每个号码概率相等**: 无论历史表现如何，每期中奖概率相同
- 🎰 **随机性**: 彩票开奖是完全随机的独立事件

### 3. 理性购彩提醒

- ✅ 固定号码分析仅供娱乐参考
- ✅ 请勿过度依赖历史数据分析
- ✅ 量力而行，理性购彩
- ❌ 不承诺中奖，不提供投注建议

---

## 🔧 在代码中使用

### Python API

```python
from scripts.generate_fixed_numbers import LotteryPredictor

# 创建预测器
predictor = LotteryPredictor('ssq')  # 或 'dlt'

# 分析固定号码
fixed_red = [7, 18, 25]
fixed_blue = [14]

result = predictor.analyze_fixed_numbers(fixed_red, fixed_blue)

# 获取分析结果
print(f"07号出现次数: {result['number_stats']['red_7']['count']}")
print(f"组合评分: {result['evaluation']['rating']}")

# 生成推荐组合
combinations = predictor.generate_combinations(
    fixed_red=fixed_red,
    fixed_blue=fixed_blue,
    count=5,
    mode='random'  # 或 'weighted'
)

# 生成文本报告
report = predictor.generate_report(result, combinations)
print(report)
```

### 集成到Web应用

```python
from flask import Flask, request, jsonify
from scripts.generate_fixed_numbers import LotteryPredictor

app = Flask(__name__)

@app.route('/api/analyze-fixed', methods=['POST'])
def analyze_fixed():
    data = request.json
    lottery_type = data.get('type', 'ssq')
    fixed_red = data.get('red', [])
    fixed_blue = data.get('blue', [])
    
    predictor = LotteryPredictor(lottery_type)
    result = predictor.analyze_fixed_numbers(fixed_red, fixed_blue)
    
    return jsonify(result)

@app.route('/api/generate-combinations', methods=['POST'])
def generate_combinations():
    data = request.json
    lottery_type = data.get('type', 'ssq')
    fixed_red = data.get('red', [])
    fixed_blue = data.get('blue', [])
    count = data.get('count', 5)
    
    predictor = LotteryPredictor(lottery_type)
    combinations = predictor.generate_combinations(
        fixed_red, fixed_blue, count
    )
    
    return jsonify(combinations)
```

---

## 📈 示例输出

### 完整分析报告

```markdown
## 🔢 双色球固定号码分析报告

### 用户输入
- 彩票类型: 双色球
- 固定红球: 07, 18, 25
- 固定蓝球: 14

---

### 📊 固定号码历史表现

| 号码 | 类型 | 出现次数 | 频率 | 当前遗漏 | 状态 |
|------|------|----------|------|----------|------|
| 07 | 红球 | 44次 | 18.11% | 1期 | 🔥 热号 |
| 18 | 红球 | 48次 | 19.75% | 12期 | 🔥 热号 |
| 25 | 红球 | 49次 | 20.16% | 7期 | 🔥 热号 |
| 14 | 蓝球 | 13次 | 5.35% | 22期 | 正常 |

**分析解读**:
- ✅ 您的3个红球都是热号，历史表现良好
- ⚠️ 07号刚开出1期，可能短期内不会重复
- ⚠️ 18号已遗漏12期，接近平均遗漏周期
- ⚠️ 14号蓝球已遗漏22期，属于较冷状态

### ⚖️ 组合合理性评估

**当前组合特征:**
- 奇偶比: 2:1（07奇, 18偶, 25奇）
- 大小比: 2:1（07小, 18大, 25大）
- 已选红球: 3个（需补充3个）
- 已选蓝球: 1个（完整）

**评估结果:** ⭐⭐⭐⭐⭐ (4/4分)

**评估解读**:
- ✅ 奇偶比2:1很好，建议补充时保持3:3或4:2
- ✅ 大小比2:1很好，建议补充时保持3:3或4:2

### 🎲 推荐组合（娱乐性质）

基于您的固定号码，以下是生成的娱乐性组合：

#### 组合 1
🔴 红球: 07 17 18 20 23 25
🔵 蓝球: 14

**补充号码分析**:
- 17号: 中等热度，已遗漏5期
- 20号: 热号，平衡大小
- 23号: 补充奇数

#### 组合 2
🔴 红球: 07 12 18 24 25 30
🔵 蓝球: 14

#### 组合 3
🔴 红球: 07 18 21 25 28 33
🔵 蓝球: 14

---

⚠️ **重要声明**:
以上分析仅供娱乐参考，不构成投注建议。彩票开奖是完全随机的独立事件，历史数据对未来开奖没有任何预测价值。每个号码在每期中奖概率相等。请理性购彩，量力而行。

生成时间: 2026-02-08 00:33:16
```

---

## ❓ 常见问题

### Q1: 固定号码最多可以选几个？
**A**: 
- 双色球红球：最多6个（选6个就是直接投注这组号码）
- 双色球蓝球：1个
- 大乐透前区：最多5个
- 大乐透后区：最多2个

### Q2: 什么是"热号权重模式"？
**A**: 在生成补充号码时，优先选择历史上出现频率较高的号码。但这只是娱乐性质的偏好设置，不影响实际中奖概率。

### Q3: 为什么我的号码显示为"冷号"？
**A**: 如果号码在历史数据中出现频率较低（<8%），会标记为冷号。但这只是历史统计，不代表未来不会开出。

### Q4: 遗漏值是什么意思？
**A**: 遗漏值表示该号码距离上次开出已经间隔了多少期。例如"遗漏12期"表示已经有12期没开出这个号码了。

### Q5: 组合评分有什么用？
**A**: 评分用于评估您选择的固定号码在奇偶比和大小比上的平衡性。平衡的组合在视觉上更"和谐"，但不影响中奖概率。

---

**最后更新**: 2026-02-08
